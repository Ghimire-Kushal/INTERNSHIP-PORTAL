import { Link } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
export default function DashboardPage() { const { user, logout } = useAuth(); const destination = user.role === "employer" ? "/employer/company" : "/profile"; return <main className="page"><nav><Link className="brand" to="/">CareerBridge</Link><div><Link to={destination}>{user.role === "employer" ? "Company" : "Profile"}</Link>{user.role === "employer" && <Link to="/employer/jobs/create">Post job</Link>}<button className="link-button" onClick={logout}>Log out</button></div></nav><section className="hero"><p className="eyebrow">{user.role.toUpperCase()} DASHBOARD</p><h1>Welcome, {user.first_name || user.username}.</h1><p className="intro">Your account is authenticated and ready for the next portal features.</p></section></main>; }
